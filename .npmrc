# Enforce exact Node.js and npm version requirements from package.json engines field
engine-strict=true

# Save exact package versions (1.2.3) instead of ranges (^1.2.3)
save-exact=true

# Disable npm funding messages after installs
fund=false
